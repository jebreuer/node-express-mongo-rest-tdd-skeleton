{
  "name": "node-express-mongo-rest-tdd-skeleton",
  "version": "1.0.0",
  "description": "An example application that demonstrates a TDD approach to building a MongoDB backed REST API. Different test environments (e.g. test, integration) are supported.",
  "main": "server.js",
  "author": "Jens Breuer",
  "license": "0BSD",
  "scripts": {
    "start": "NODE_ENV=dev node server.js",
    "test": "nyc --reporter=html --reporter=text mocha --exit --timeout 10000"
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "config": "^3.0.1",
    "express": "^4.16.4",
    "mongoose": "^5.4.7",
    "morgan": "^1.9.1"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.2.1",
    "mocha": "^5.2.0",
    "mongodb-memory-server": "^3.0.0",
    "nyc": "^13.1.0",
    "standard": "^12.0.1"
  }
}